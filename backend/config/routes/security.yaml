# config/routes/security.yaml
_security_logout:
    path: /logout
    controller: Symfony\Bundle\SecurityBundle\Controller\LogoutController
    defaults:
        # Redirige al home después del logout (cambia según tu app)
        _target_path: /
    methods: [GET, POST]
