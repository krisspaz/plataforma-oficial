# config/routes.yaml

# Carga automática de todos los controllers usando atributos
controllers:
    resource: '../src/Controller/'
    type: attribute

# Ruta para login vía API (JWT)
api_login_check:
    path: /api/login_check
    methods: [POST]

# Opcional: asegurar que todas las rutas de la API estén bajo /api
# api_platform:
#     resource: '.'
#     type: api_platform
#     prefix: /api
